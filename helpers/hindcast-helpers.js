const axios = require('axios');

/**
 * @param {string} stationID Water service of canada station id number 
 */
const fetchRealtimeHindcast = (stationID) => {

}

module.exports = fetchRealtimeHindcast;